<Window x:Class="CancellationTokenWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CancellationToken demo" Height="260" Width="420">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Timeout (seconds):" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox x:Name="TimeoutTextBox" Width="60" Text="30" />
        </StackPanel>

        <CheckBox x:Name="UseAsyncWorkCheckBox" Grid.Row="1" Margin="0,0,0,12"
                  Content="Use async work (I/O-bound, Task.Delay)" />

        <ProgressBar x:Name="WorkProgressBar" Grid.Row="2" Height="20" Margin="0,0,0,8" Maximum="50" />

        <TextBlock x:Name="StatusText" Grid.Row="3" Text="Ready" Margin="0,0,0,12" />

        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <Button x:Name="StartButton" Content="Start" Width="75" Margin="0,0,8,0" Click="StartButton_Click" />
            <Button x:Name="CancelButton" Content="Cancel" Width="75" IsEnabled="False" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
